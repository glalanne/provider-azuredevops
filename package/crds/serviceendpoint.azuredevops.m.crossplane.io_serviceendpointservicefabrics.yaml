---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceendpointservicefabrics.serviceendpoint.azuredevops.m.crossplane.io
spec:
  group: serviceendpoint.azuredevops.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - azuredevops
    kind: ServiceendpointServicefabric
    listKind: ServiceendpointServicefabricList
    plural: serviceendpointservicefabrics
    singular: serviceendpointservicefabric
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceendpointServicefabric is the Schema for the ServiceendpointServicefabrics
          API. Manages a Service Fabric service endpoint Azure DevOps organization.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceendpointServicefabricSpec defines the desired state
              of ServiceendpointServicefabric
            properties:
              forProvider:
                properties:
                  azureActiveDirectory:
                    description: An azure_active_directory block as documented below.
                    properties:
                      passwordSecretRef:
                        description: |-
                          - Password for the Azure Active Directory account.
                          Password for the Azure Active Directory account.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      serverCertificateCommonName:
                        description: |-
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                        type: string
                      serverCertificateLookup:
                        description: 'Verification mode for the cluster. Possible
                          values are: Thumbprint, CommonName.'
                        type: string
                      serverCertificateThumbprint:
                        description: |-
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                        type: string
                      username:
                        description: |-
                          - Specify an Azure Active Directory account.
                          Specify an Azure Active Directory account.
                        type: string
                    type: object
                  certificate:
                    description: A certificate block as documented below.
                    properties:
                      clientCertificatePasswordSecretRef:
                        description: |-
                          Password for the certificate.
                          Password for the certificate.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificateSecretRef:
                        description: |-
                          Base64 encoding of the cluster's client certificate file.
                          Base64 encoding of the cluster's client certificate file.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      serverCertificateCommonName:
                        description: |-
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                        type: string
                      serverCertificateLookup:
                        description: 'Verification mode for the cluster. Possible
                          values are: Thumbprint, CommonName.'
                        type: string
                      serverCertificateThumbprint:
                        description: |-
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                        type: string
                    type: object
                  clusterEndpoint:
                    description: |-
                      Client connection endpoint for the cluster. Prefix the value with tcp://;. This value overrides the publish profile.
                      Client connection endpoint for the cluster. Prefix the value with 'tcp://';. This value overrides the publish profile.
                    type: string
                  description:
                    description: The Service Endpoint description.
                    type: string
                  none:
                    description: A none block as documented below.
                    properties:
                      clusterSpn:
                        description: |-
                          Fully qualified domain SPN for gMSA account. This is applicable only if unsecured option is disabled.
                          Fully qualified domain SPN for gMSA account. This is applicable only if `unsecured` option is disabled.
                        type: string
                      unsecured:
                        description: |-
                          Skip using windows security for authentication.
                          Skip using windows security for authentication.
                        type: boolean
                    type: object
                  projectId:
                    description: The ID of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceEndpointName:
                    description: The Service Endpoint name.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  azureActiveDirectory:
                    description: An azure_active_directory block as documented below.
                    properties:
                      passwordSecretRef:
                        description: |-
                          - Password for the Azure Active Directory account.
                          Password for the Azure Active Directory account.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      serverCertificateCommonName:
                        description: |-
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                        type: string
                      serverCertificateLookup:
                        description: 'Verification mode for the cluster. Possible
                          values are: Thumbprint, CommonName.'
                        type: string
                      serverCertificateThumbprint:
                        description: |-
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                        type: string
                      username:
                        description: |-
                          - Specify an Azure Active Directory account.
                          Specify an Azure Active Directory account.
                        type: string
                    required:
                    - passwordSecretRef
                    type: object
                  certificate:
                    description: A certificate block as documented below.
                    properties:
                      clientCertificatePasswordSecretRef:
                        description: |-
                          Password for the certificate.
                          Password for the certificate.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      clientCertificateSecretRef:
                        description: |-
                          Base64 encoding of the cluster's client certificate file.
                          Base64 encoding of the cluster's client certificate file.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      serverCertificateCommonName:
                        description: |-
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                        type: string
                      serverCertificateLookup:
                        description: 'Verification mode for the cluster. Possible
                          values are: Thumbprint, CommonName.'
                        type: string
                      serverCertificateThumbprint:
                        description: |-
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                        type: string
                    required:
                    - clientCertificateSecretRef
                    type: object
                  clusterEndpoint:
                    description: |-
                      Client connection endpoint for the cluster. Prefix the value with tcp://;. This value overrides the publish profile.
                      Client connection endpoint for the cluster. Prefix the value with 'tcp://';. This value overrides the publish profile.
                    type: string
                  description:
                    description: The Service Endpoint description.
                    type: string
                  none:
                    description: A none block as documented below.
                    properties:
                      clusterSpn:
                        description: |-
                          Fully qualified domain SPN for gMSA account. This is applicable only if unsecured option is disabled.
                          Fully qualified domain SPN for gMSA account. This is applicable only if `unsecured` option is disabled.
                        type: string
                      unsecured:
                        description: |-
                          Skip using windows security for authentication.
                          Skip using windows security for authentication.
                        type: boolean
                    type: object
                  projectId:
                    description: The ID of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceEndpointName:
                    description: The Service Endpoint name.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.clusterEndpoint is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clusterEndpoint)
                || (has(self.initProvider) && has(self.initProvider.clusterEndpoint))'
            - message: spec.forProvider.serviceEndpointName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceEndpointName)
                || (has(self.initProvider) && has(self.initProvider.serviceEndpointName))'
          status:
            description: ServiceendpointServicefabricStatus defines the observed state
              of ServiceendpointServicefabric.
            properties:
              atProvider:
                properties:
                  authorization:
                    additionalProperties:
                      type: string
                    type: object
                    x-kubernetes-map-type: granular
                  azureActiveDirectory:
                    description: An azure_active_directory block as documented below.
                    properties:
                      serverCertificateCommonName:
                        description: |-
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                        type: string
                      serverCertificateLookup:
                        description: 'Verification mode for the cluster. Possible
                          values are: Thumbprint, CommonName.'
                        type: string
                      serverCertificateThumbprint:
                        description: |-
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                        type: string
                      username:
                        description: |-
                          - Specify an Azure Active Directory account.
                          Specify an Azure Active Directory account.
                        type: string
                    type: object
                  certificate:
                    description: A certificate block as documented below.
                    properties:
                      serverCertificateCommonName:
                        description: |-
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                          The common name(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple common names with a comma (',')
                        type: string
                      serverCertificateLookup:
                        description: 'Verification mode for the cluster. Possible
                          values are: Thumbprint, CommonName.'
                        type: string
                      serverCertificateThumbprint:
                        description: |-
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                          The thumbprint(s) of the cluster's certificate(s). This is used to verify the identity of the cluster. This value overrides the publish profile. Separate multiple thumbprints with a comma (',')
                        type: string
                    type: object
                  clusterEndpoint:
                    description: |-
                      Client connection endpoint for the cluster. Prefix the value with tcp://;. This value overrides the publish profile.
                      Client connection endpoint for the cluster. Prefix the value with 'tcp://';. This value overrides the publish profile.
                    type: string
                  description:
                    description: The Service Endpoint description.
                    type: string
                  id:
                    description: The ID of the service endpoint.
                    type: string
                  none:
                    description: A none block as documented below.
                    properties:
                      clusterSpn:
                        description: |-
                          Fully qualified domain SPN for gMSA account. This is applicable only if unsecured option is disabled.
                          Fully qualified domain SPN for gMSA account. This is applicable only if `unsecured` option is disabled.
                        type: string
                      unsecured:
                        description: |-
                          Skip using windows security for authentication.
                          Skip using windows security for authentication.
                        type: boolean
                    type: object
                  projectId:
                    description: The ID of the project.
                    type: string
                  serviceEndpointName:
                    description: The Service Endpoint name.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
