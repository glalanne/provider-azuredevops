---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: serviceendpointazurerms.serviceendpoint.azuredevops.m.crossplane.io
spec:
  group: serviceendpoint.azuredevops.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - azuredevops
    kind: ServiceendpointAzurerm
    listKind: ServiceendpointAzurermList
    plural: serviceendpointazurerms
    singular: serviceendpointazurerm
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServiceendpointAzurerm is the Schema for the ServiceendpointAzurerms
          API. Manages a Azure Resource Manager service endpoint within Azure DevOps
          organization.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ServiceendpointAzurermSpec defines the desired state of ServiceendpointAzurerm
            properties:
              forProvider:
                properties:
                  azurermManagementGroupId:
                    description: |-
                      The Management group ID of the Azure targets.
                      The Azure managementGroup Id which should be used.
                    type: string
                  azurermManagementGroupName:
                    description: |-
                      The Management group Name of the targets.
                      The Azure managementGroup name which should be used.
                    type: string
                  azurermSpnTenantid:
                    description: |-
                      The Tenant ID of the service principal.
                      The service principal tenant id which should be used.
                    type: string
                  azurermSubscriptionId:
                    description: |-
                      The Subscription ID of the Azure targets.
                      The Azure subscription Id which should be used.
                    type: string
                  azurermSubscriptionName:
                    description: |-
                      The Subscription Name of the targets.
                      The Azure subscription name which should be used.
                    type: string
                  credentials:
                    description: A credentials block as defined below.
                    properties:
                      serviceprincipalcertificateSecretRef:
                        description: The service principal certificate. This not required
                          if service_endpoint_authentication_scheme is set to WorkloadIdentityFederation.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      serviceprincipalid:
                        description: |-
                          The service principal application ID
                          The service principal id which should be used.
                        type: string
                      serviceprincipalkeySecretRef:
                        description: The service principal secret. This not required
                          if service_endpoint_authentication_scheme is set to WorkloadIdentityFederation.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  description:
                    description: Service connection description.
                    type: string
                  environment:
                    description: |-
                      The Cloud Environment to use. Defaults to AzureCloud. Possible values are AzureCloud, AzureChinaCloud, AzureUSGovernment, AzureGermanCloud and AzureStack. Changing this forces a new resource to be created.
                      Environment (Azure Cloud type)
                    type: string
                  features:
                    description: A features block as defined below.
                    properties:
                      validate:
                        description: |-
                          Whether or not to validate connection with Azure after create or update operations. Defaults to false
                          Whether or not to validate connection with azure after create or update operations
                        type: boolean
                    type: object
                  projectId:
                    description: The ID of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  resourceGroup:
                    description: |-
                      The resource group used for scope of automatic service endpoint.
                      Scope Resource Group
                    type: string
                  serverUrl:
                    description: The server URL of the service endpoint. Changing
                      this forces a new resource to be created.
                    type: string
                  serviceEndpointAuthenticationScheme:
                    description: |-
                      Specifies the type of Azure Resource Manager Service Endpoint. Possible values are WorkloadIdentityFederation, ManagedServiceIdentity or ServicePrincipal. Defaults to ServicePrincipal for backwards compatibility.
                      The AzureRM Service Endpoint Authentication Scheme, this can be 'WorkloadIdentityFederation', 'ManagedServiceIdentity' or 'ServicePrincipal'.
                    type: string
                  serviceEndpointName:
                    description: The Service Endpoint Name.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  azurermManagementGroupId:
                    description: |-
                      The Management group ID of the Azure targets.
                      The Azure managementGroup Id which should be used.
                    type: string
                  azurermManagementGroupName:
                    description: |-
                      The Management group Name of the targets.
                      The Azure managementGroup name which should be used.
                    type: string
                  azurermSpnTenantid:
                    description: |-
                      The Tenant ID of the service principal.
                      The service principal tenant id which should be used.
                    type: string
                  azurermSubscriptionId:
                    description: |-
                      The Subscription ID of the Azure targets.
                      The Azure subscription Id which should be used.
                    type: string
                  azurermSubscriptionName:
                    description: |-
                      The Subscription Name of the targets.
                      The Azure subscription name which should be used.
                    type: string
                  credentials:
                    description: A credentials block as defined below.
                    properties:
                      serviceprincipalcertificateSecretRef:
                        description: The service principal certificate. This not required
                          if service_endpoint_authentication_scheme is set to WorkloadIdentityFederation.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      serviceprincipalid:
                        description: |-
                          The service principal application ID
                          The service principal id which should be used.
                        type: string
                      serviceprincipalkeySecretRef:
                        description: The service principal secret. This not required
                          if service_endpoint_authentication_scheme is set to WorkloadIdentityFederation.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  description:
                    description: Service connection description.
                    type: string
                  environment:
                    description: |-
                      The Cloud Environment to use. Defaults to AzureCloud. Possible values are AzureCloud, AzureChinaCloud, AzureUSGovernment, AzureGermanCloud and AzureStack. Changing this forces a new resource to be created.
                      Environment (Azure Cloud type)
                    type: string
                  features:
                    description: A features block as defined below.
                    properties:
                      validate:
                        description: |-
                          Whether or not to validate connection with Azure after create or update operations. Defaults to false
                          Whether or not to validate connection with azure after create or update operations
                        type: boolean
                    type: object
                  projectId:
                    description: The ID of the project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in project to populate projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in project to populate projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  resourceGroup:
                    description: |-
                      The resource group used for scope of automatic service endpoint.
                      Scope Resource Group
                    type: string
                  serverUrl:
                    description: The server URL of the service endpoint. Changing
                      this forces a new resource to be created.
                    type: string
                  serviceEndpointAuthenticationScheme:
                    description: |-
                      Specifies the type of Azure Resource Manager Service Endpoint. Possible values are WorkloadIdentityFederation, ManagedServiceIdentity or ServicePrincipal. Defaults to ServicePrincipal for backwards compatibility.
                      The AzureRM Service Endpoint Authentication Scheme, this can be 'WorkloadIdentityFederation', 'ManagedServiceIdentity' or 'ServicePrincipal'.
                    type: string
                  serviceEndpointName:
                    description: The Service Endpoint Name.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.azurermSpnTenantid is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.azurermSpnTenantid)
                || (has(self.initProvider) && has(self.initProvider.azurermSpnTenantid))'
            - message: spec.forProvider.serviceEndpointName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.serviceEndpointName)
                || (has(self.initProvider) && has(self.initProvider.serviceEndpointName))'
          status:
            description: ServiceendpointAzurermStatus defines the observed state of
              ServiceendpointAzurerm.
            properties:
              atProvider:
                properties:
                  authorization:
                    additionalProperties:
                      type: string
                    type: object
                    x-kubernetes-map-type: granular
                  azurermManagementGroupId:
                    description: |-
                      The Management group ID of the Azure targets.
                      The Azure managementGroup Id which should be used.
                    type: string
                  azurermManagementGroupName:
                    description: |-
                      The Management group Name of the targets.
                      The Azure managementGroup name which should be used.
                    type: string
                  azurermSpnTenantid:
                    description: |-
                      The Tenant ID of the service principal.
                      The service principal tenant id which should be used.
                    type: string
                  azurermSubscriptionId:
                    description: |-
                      The Subscription ID of the Azure targets.
                      The Azure subscription Id which should be used.
                    type: string
                  azurermSubscriptionName:
                    description: |-
                      The Subscription Name of the targets.
                      The Azure subscription name which should be used.
                    type: string
                  credentials:
                    description: A credentials block as defined below.
                    properties:
                      serviceprincipalid:
                        description: |-
                          The service principal application ID
                          The service principal id which should be used.
                        type: string
                    type: object
                  description:
                    description: Service connection description.
                    type: string
                  environment:
                    description: |-
                      The Cloud Environment to use. Defaults to AzureCloud. Possible values are AzureCloud, AzureChinaCloud, AzureUSGovernment, AzureGermanCloud and AzureStack. Changing this forces a new resource to be created.
                      Environment (Azure Cloud type)
                    type: string
                  features:
                    description: A features block as defined below.
                    properties:
                      validate:
                        description: |-
                          Whether or not to validate connection with Azure after create or update operations. Defaults to false
                          Whether or not to validate connection with azure after create or update operations
                        type: boolean
                    type: object
                  id:
                    description: The ID of the service endpoint.
                    type: string
                  projectId:
                    description: The ID of the project.
                    type: string
                  resourceGroup:
                    description: |-
                      The resource group used for scope of automatic service endpoint.
                      Scope Resource Group
                    type: string
                  serverUrl:
                    description: The server URL of the service endpoint. Changing
                      this forces a new resource to be created.
                    type: string
                  serviceEndpointAuthenticationScheme:
                    description: |-
                      Specifies the type of Azure Resource Manager Service Endpoint. Possible values are WorkloadIdentityFederation, ManagedServiceIdentity or ServicePrincipal. Defaults to ServicePrincipal for backwards compatibility.
                      The AzureRM Service Endpoint Authentication Scheme, this can be 'WorkloadIdentityFederation', 'ManagedServiceIdentity' or 'ServicePrincipal'.
                    type: string
                  serviceEndpointName:
                    description: The Service Endpoint Name.
                    type: string
                  servicePrincipalId:
                    description: The Application(Client) ID of the Service Principal.
                    type: string
                  workloadIdentityFederationIssuer:
                    description: |-
                      The issuer if service_endpoint_authentication_scheme is set to WorkloadIdentityFederation. This looks like https://vstoken.dev.azure.com/00000000-0000-0000-0000-000000000000, where the GUID is the Organization ID of your Azure DevOps Organisation.
                      The issuer of the workload identity federation service principal.
                    type: string
                  workloadIdentityFederationSubject:
                    description: |-
                      The subject if service_endpoint_authentication_scheme is set to WorkloadIdentityFederation. This looks like sc://<organisation>/<project>/<service-connection-name>.
                      The subject of the workload identity federation service principal.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
